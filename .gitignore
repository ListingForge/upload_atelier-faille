node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

data/*
!data/pricing.json
!data/printify-catalog.json
# mockup-lists.json is user-editable at runtime (order, uploads) and lives only
# on the server so live edits survive deploys.
